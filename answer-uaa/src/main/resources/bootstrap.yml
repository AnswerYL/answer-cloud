server:
  port: 8000
  servlet:
    session:
      cookie:
        name: OAUTH2SESSION

spring:
  application:
    name: uaa-server

encrypt:
  key-store:
    location: classpath:answer.jks
    secret: answer!@#$
    alias: answer